export {
    AlertTriangle, RefreshCw, //ErrorBoundary
    X, Key, ExternalLink, Moon, Sun, Monitor, Map, //AppSettings
    Folder, Loader2, //Map, //SaveAs
    Palette, //X, //MapSettings
    Search, MapPin, //X, Loader2, //LocationSearch
    CheckCircle, XCircle, Info, PlayCircle, //Loader2, AlertTriangle, //RenderOverlay
    Mic, Sparkles, Settings2, Loader, //ScriptInput
    //Info, //ScrubInput
    Menu, Minus, Square, ChevronRight, PanelBottom, Undo2, Redo2, //X, Map, Settings2 //TitleBar
    Car, Footprints, Ruler, Plane, Trash2, Edit, CopyPlus, CornerDownLeft, Ship, //WaypointLayer
    UploadCloud, //MapPin //MapArea
    FileVideo, Map as MapIcon, Route, Play, PencilSparkles, MapPinIcon, Clapperboard, Hourglass, ToolCase, ChevronDown, // ChevronRight, Trash2, Car, Footprints, Ruler, Plane, Square, Mic, Sparkles, Settings2, Ship, Loader, //Sidebar
    Film, SkipBack, SkipForward, Volume2, Maximize,PictureInPicture, // Play,  Settings2, //VideoArea
    ChevronLeft, Image as ImageIcon, // X, Trash2, MapPin, Settings2,Mic, //WaypointEditor
    GripVertical, Edit2, //X, Image as ImageIcon, Mic //WaypointItem
    FolderPlus, Clock, // MapPin, Monitor, ChevronRight, //NewProject
    Plus, FolderOpen, LayoutGrid, List, //Map, Clock //TitleScreen
    Cpu, Check, // EngineSelect
    Pause, Scissors, Magnet, MousePointer2, Type, Video, Download, Subtitles, // TimelineView
    File, FileAudio, FolderSync, // MediaPool
    ZoomIn, ZoomOut, // TimelineView
    Layers, //TimelineTrack
    MonitorPlay, //ExportPanel
    CheckCircle2, AlertCircle, // Toast
    Copy, CircleDashed, History, Bell, // StatusBar
    Settings, Save, // AppSettings
    Pencil, SplinePointer, Undo, Eraser, MapPinPen  // MapArea 
} from "lucide-react";